- remove the email admin/usder email - replace with a username instead: SEED_ADMIN_EMAIL=admin@example.com; SEED_ADMIN_PASSWORD=admin123

- remove the current backend local RAG-like semantic retrieval path that does not require a LightRAG server becuase we will ONLY use the LightRAG server.
Use Concept C — LightRAG domain deployment manager 