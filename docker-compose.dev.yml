services:
  migrate:
    volumes:
      - .:/app

  api:
    command: uvicorn app.main:create_app --factory --host 0.0.0.0 --port ${API_PORT:-8010} --reload
    volumes:
      - .:/app
      - uploads:/app/.data/uploads

  worker:
    volumes:
      - .:/app
      - uploads:/app/.data/uploads
